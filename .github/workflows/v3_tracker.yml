name: V3 Hourly Tracker

on:
  schedule:
    # 07:00-20:00 UTC = 15:00-04:00 Bali (UTC+8)
    # EU apre 07:00 UTC (09:00 CEST), USA chiude 20:00 UTC (16:00 EDT)
    # Lunedì-Venerdì
    - cron: '0 7-20 * * 1-5'
  workflow_dispatch:

jobs:
  tracker:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt numpy

      - name: Run tracker_v3.py
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python tracker_v3.py

      - name: Commit portfolio_v3.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add portfolio_v3.json "Portfolio V3 Status.md"
          git diff --cached --quiet || git commit -m "chore: update portfolio_v3 [tracker $(date +'%Y-%m-%d %H:%M')]"
          git push
